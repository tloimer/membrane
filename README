Get up running:

cd .../tests;
matlab &
>> runalltests;
% See runalltests.m in tests/ and files called therein, e.g., testmnumadiabat.m

Go up hacking:

$ mkdir /tmp/aa; cd ~/asym/matlab
$ cp -a * /tmp/aa
$ cd /tmp/aa
$ rm -r tests/12*
$ git init --separate-git-dir ~/matlab.git
$ .
$ .
$ .
$ git status; git log; git show; git diff; git diff --cached;
$ git log --oneline --graph --decorate --all -40



____folder structure____

Program code, or core program, in matlab/program.
Utility, e.g., output-functions, remain in matlab/. Probably, when these
functions are often reused, they may also go into program/. Probably, in future,
there may be a folder matlab/outputfunctions/.
Results of calculations stay in matlab/. This is the dirty folder.


____git____

Use git rather sparingly, do not think too much about it.
The master branch only contains the consolidated versions of the program.
Development occurs in the release branches, named e.g. asym. The branch complete
is temporary and holds all the figures. Figures are kept in the working
directory, anyway, no need to control their version.

            master
               |
  09eurotherm  o
               |
               |
  12potsdam    o
               |\ branch
               | \ asym
               |  o
               |  |\ branch
               |  o \ complete
               |  |  o
               |  o  |
               |  |\ |
               |  | \|
               |  o  o
               | / \ |
               |/   \|
  asym         o     o

i.e., workflow:
git checkout -b asym
 hack..hack..hack
git commit .. git commit
git checkout master
git merge --no-ff [--no-commit] asym
# creates a commit with the merge-message "Merge branch 'asym'". Probably use
git merge --no-ff -m 'Version asym' [--no-commit] asym
git tag 'asym'


__Kern des matlab-Programms, aus ~/projects/11jms/matlab3 kopiert:
  flow12.m fmodel.m membrane.m mlinear.m mnum.m substance.m
  misotherm.m, Kopie von 12potsdam/calc/matlab/misotherm.m
  zugefügt: findinterval.m, flowsetup.m, flowstruct.m, mstack.m

__Eine utility-function, aus ~/12potsdam/calc/matlab:
misotherm.m
Andere utility-functions siehe ../results.
